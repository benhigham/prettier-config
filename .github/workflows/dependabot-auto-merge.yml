name: Auto-merge Dependabot PRs

on:
  pull_request:
    types: [opened, reopened]

permissions: {}

concurrency:
  group: dependabot-auto-merge-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  auto-merge:
    if: github.actor == 'dependabot[bot]'
    permissions:
      contents: write
      pull-requests: write
    # Reusable workflow from benhigham/.github. For debugging failures, see:
    # https://github.com/benhigham/.github/blob/main/.github/workflows/auto-merge-dependabot.yml
    uses: benhigham/.github/.github/workflows/auto-merge-dependabot.yml@bbf313580f9e36aa7208f7f663a5bac44c4c2704
